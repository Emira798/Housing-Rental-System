用户模块功能文档

一、数据库与实体类设计

1. 用户表（user）：
   id：整数类型，主键，自增长，用户唯一标识
   username：字符串类型，非空，唯一，用户名
   password：字符串类型，非空，用户密码（加密存储）
   email：字符串类型，非空，唯一，用户邮箱地址
   phone：字符串类型，用户手机号码
   role_code：字符串类型，用户角色编码（ADMIN-管理员，LANDLORD-房东，TENANT-租户）
   name：字符串类型，用户姓名
   sex：字符串类型，用户性别
   id_card：字符串类型，用户身份证号码
   avatar：字符串类型，用户头像路径
   status：整数类型，用户状态（0-禁用，1-正常）
   create_time：日期时间类型，创建时间
   update_time：日期时间类型，更新时间

2. 实体类定义：
   User类：对应用户表，包含用户基本信息属性
   【springboot/src/main/java/org/example/springboot/entity/User.java】
   public class User {
     private Long id;             // 用户ID
     private String username;     // 用户名
     private String password;     // 密码
     private String email;        // 邮箱
     private String phone;        // 手机号
     private String roleCode;     // 角色编码
     private String name;         // 姓名
     private String sex;          // 性别
     private String idCard;       // 身份证号
     private String avatar;       // 头像URL
     private Integer status;      // 状态
     private LocalDateTime createTime;  // 创建时间
     private LocalDateTime updateTime;  // 更新时间
     private String token;        // 非数据库字段，用于存储登录令牌
   }

   UserPasswordUpdateDTO类：密码修改数据传输对象
   【springboot/src/main/java/org/example/springboot/DTO/UserPasswordUpdateDTO.java】
   public class UserPasswordUpdateDTO {
     private String oldPassword;  // 旧密码
     private String newPassword;  // 新密码
   }

二、用户登录功能

功能概述：
系统提供用户登录功能，支持使用用户名和密码登录，登录成功后返回包含用户信息的token。

实现流程：
1. 用户输入用户名和密码
2. 前端请求后端登录接口
3. 后端验证用户名和密码
4. 验证通过生成JWT令牌并返回用户信息和令牌
5. 前端存储用户信息和令牌，完成登录流程

关键代码示例：
【vue3/src/store/user.js：login方法】
async login(loginForm) {
  try {
    const res = await request.post('/user/login', loginForm)
    this.setUserInfo(res)
    return res
  } catch (error) {
    this.clearUserInfo()
    throw error
  }
}

【springboot/src/main/java/org/example/springboot/controller/UserController.java：login方法】
@PostMapping("/login")
public Result<?> login(@RequestBody User user) {
  User loginUser = userService.login(user);
  return Result.success(loginUser);
}

【springboot/src/main/java/org/example/springboot/service/UserService.java：login方法】
public User login(User user) {
  User dbUser = getByUsername(user.getUsername());
  if (dbUser.getStatus().equals(AccountStatus.PENDING_REVIEW.getValue())) {
    throw new ServiceException("账号正在审核");
  }
  if (dbUser.getStatus().equals(AccountStatus.REVIEW_FAILED.getValue())) {
    throw new ServiceException("账号审核不通过，请修改个人信息");
  }
  if (!bCryptPasswordEncoder.matches(user.getPassword(), dbUser.getPassword())) {
    throw new ServiceException("用户名或密码错误");
  }
  String token = JwtTokenUtils.genToken(String.valueOf(dbUser.getId()), dbUser.getPassword());
  dbUser.setToken(token);
  return dbUser;
}

功能实现细节：
1. 登录验证过程会检查用户状态，包括账号是否被禁用、是否在审核中等
2. 密码使用BCrypt加密算法进行验证
3. 登录成功后生成JWT令牌，并将用户信息和令牌一起返回
4. 前端将用户信息和令牌存储在LocalStorage中以保持登录状态
5. 登录失败会返回具体的错误信息

三、用户列表管理功能

功能概述：
系统提供用户列表管理功能，管理员可以查看、搜索、添加、编辑、删除和禁用/启用用户。用户管理是系统管理的核心功能，通过此功能管理员可以对系统用户进行全面管理。

实现流程：
1. 加载用户列表页面时请求后端分页接口获取用户数据
2. 支持按用户名、姓名等条件搜索用户
3. 提供添加用户、编辑用户、删除用户等操作按钮
4. 点击相应按钮弹出对话框或执行相应操作
5. 提供批量删除功能，可选择多个用户同时删除
6. 提供用户状态管理功能，可启用或禁用用户账号

关键代码示例：
【vue3/src/views/backend/user/UserList.vue：获取用户列表】
const fetchUsers = async () => {
  loading.value = true
  try {
    const res = await request.get('/user/page', {
      username: searchForm.username,
      name: searchForm.name,
      currentPage: currentPage.value,
      size: pageSize.value
    })
    tableData.value = res.records
    total.value = res.total
  } catch (error) {
    console.error('获取用户列表失败:', error)
  } finally {
    loading.value = false
  }
}

【vue3/src/views/backend/user/UserList.vue：添加用户】
const handleAdd = () => {
  dialogType.value = 'add'
  resetForm()
  dialogVisible.value = true
}

【vue3/src/views/backend/user/UserList.vue：编辑用户】
const handleEdit = (row) => {
  dialogType.value = 'edit'
  resetForm()
  Object.assign(form, row)
  dialogVisible.value = true
}

【vue3/src/views/backend/user/UserList.vue：提交表单】
const submitForm = () => {
  formRef.value.validate(async (valid) => {
    if (valid) {
      try {
        if (dialogType.value === 'add') {
          await request.post('/user/add', form)
          ElMessage.success('添加成功')
        } else {
          await request.put(`/user/${form.id}`, form)
          ElMessage.success('更新成功')
        }
        dialogVisible.value = false
        fetchUsers()
      } catch (error) {
        console.error('保存用户失败:', error)
      }
    }
  })
}

【vue3/src/views/backend/user/UserList.vue：删除用户】
const handleDelete = async (id) => {
  try {
    await request.delete(`/user/delete/${id}`)
    ElMessage.success('删除成功')
    fetchUsers()
  } catch (error) {
    console.error('删除用户失败:', error)
  }
}

【vue3/src/views/backend/user/UserList.vue：批量删除】
const handleBatchDelete = async () => {
  if (selectedIds.value.length === 0) {
    ElMessage.warning('请选择要删除的用户')
    return
  }
  
  try {
    await ElMessageBox.confirm('确定要删除选中的用户吗？', '提示', {
      type: 'warning'
    })
    await request.delete('/user/deleteBatch', {
      params: { ids: selectedIds.value }
    })
    ElMessage.success('批量删除成功')
    fetchUsers()
  } catch (error) {
    if (error !== 'cancel') {
      console.error('批量删除失败:', error)
    }
  }
}

【vue3/src/views/backend/user/UserList.vue：重置密码】
const handleResetPassword = async (row) => {
  try {
    await ElMessageBox.prompt('请输入新密码', '重置密码', {
      confirmButtonText: '确定',
      cancelButtonText: '取消',
      inputType: 'password',
      inputPattern: /.{6,20}/,
      inputErrorMessage: '密码长度在6-20个字符之间'
    })
    .then(async ({ value }) => {
      await request.get('/user/forget', {
        params: {
          email: row.email,
          newPassword: value
        }
      })
      ElMessage.success('密码重置成功')
    })
  } catch (error) {
    if (error !== 'cancel') {
      console.error('重置密码失败:', error)
    }
  }
}

【springboot/src/main/java/org/example/springboot/controller/UserController.java：分页查询用户】
@GetMapping("/page")
public Result<?> getUsersByPage(
  @RequestParam(defaultValue = "") String username,
  @RequestParam(defaultValue = "") String name,
  @RequestParam(defaultValue = "") String roleCode,
  @RequestParam(defaultValue = "1") Integer currentPage,
  @RequestParam(defaultValue = "10") Integer size) {
  Page<User> page = userService.getUsersByPage(username, name, roleCode, currentPage, size);
  return Result.success(page);
}

【springboot/src/main/java/org/example/springboot/controller/UserController.java：创建新用户】
@PostMapping("/add")
public Result<?> createUser(@RequestBody User user) {
  userService.createUser(user);
  return Result.success("创建成功");
}

【springboot/src/main/java/org/example/springboot/controller/UserController.java：更新用户信息】
@PutMapping("/{id}")
public Result<?> updateUser(@PathVariable Long id, @RequestBody User user) {
  userService.updateUser(id, user);
  return Result.success("更新成功");
}

【springboot/src/main/java/org/example/springboot/controller/UserController.java：删除用户】
@DeleteMapping("/delete/{id}")
public Result<?> deleteUserById(@PathVariable Long id) {
  userService.deleteUserById(id);
  return Result.success("删除成功");
}

【springboot/src/main/java/org/example/springboot/controller/UserController.java：批量删除用户】
@DeleteMapping("/deleteBatch")
public Result<?> deleteBatch(@RequestParam List<Integer> ids) {
  userService.deleteBatch(ids);
  return Result.success("批量删除成功");
}

【springboot/src/main/java/org/example/springboot/service/UserService.java：分页查询用户】
public Page<User> getUsersByPage(String username, String name, String roleCode, Integer currentPage, Integer size) {
  LambdaQueryWrapper<User> queryWrapper = new LambdaQueryWrapper<>();
  
  // 添加查询条件
  if (StringUtils.isNotBlank(username)) {
    queryWrapper.like(User::getUsername, username);
  }
  if (StringUtils.isNotBlank(name)) {
    queryWrapper.like(User::getName, name);
  }
  if (StringUtils.isNotBlank(roleCode)) {
    queryWrapper.eq(User::getRoleCode, roleCode);
  }
  
  return userMapper.selectPage(new Page<>(currentPage, size), queryWrapper);
}

功能实现细节：
1. 用户列表采用分页展示，支持调整每页显示数量（10, 20, 50, 100条/页）
2. 实现表格顶部搜索区域，包含以下搜索条件：
   - 用户名：支持模糊搜索
   - 姓名：支持模糊搜索
   - 角色：可按角色类型筛选（管理员、房东、租户）
3. 表格展示用户主要信息：
   - ID
   - 用户名
   - 姓名
   - 性别
   - 邮箱
   - 手机号
   - 身份证号
   - 头像（显示缩略图）
   - 状态（正常/禁用）
   - 创建时间
   - 操作按钮
4. 用户添加功能：
   - 弹出添加用户对话框
   - 表单包含必填字段验证（用户名、密码、邮箱）
   - 自动检查用户名和邮箱是否已存在
   - 支持上传用户头像
   - 可设置初始状态（正常/禁用）
5. 用户编辑功能：
   - 弹出编辑用户对话框，预填充用户当前信息
   - 用户名字段禁用修改
   - 支持更新用户基本信息和头像
   - 编辑时不需要输入密码
6. 删除功能：
   - 单个删除：点击删除按钮，弹出确认对话框
   - 批量删除：选择多个用户，点击批量删除按钮
   - 删除操作不可恢复，会从数据库中永久删除用户数据
7. 批量操作功能：
   - 表格左侧提供复选框，可选择多条记录
   - 批量删除：删除所有选中的用户
8. 状态管理功能：
   - 通过不同颜色标签显示用户状态（绿色-正常，灰色-禁用）
   - 点击"禁用/启用"按钮可切换用户状态
   - 状态变更立即生效，影响用户登录权限
9. 重置密码功能：
   - 管理员可以为任意用户重置密码
   - 点击"重置密码"按钮，弹出输入框
   - 输入新密码后直接修改，无需原密码验证
   - 密码长度必须在6-20个字符之间
10. 用户头像管理：
    - 添加和编辑用户时可上传头像
    - 支持JPG和PNG格式，大小限制2MB以内
    - 用户头像显示在表格中，便于识别用户
11. 数据验证与安全性：
    - 所有表单输入进行前端和后端双重验证
    - 邮箱格式验证
    - 手机号格式验证（1开头的11位数字）
    - 身份证号格式验证
    - 密码加密存储，使用BCrypt算法

界面交互体验：
1. 表格数据加载时显示加载动画
2. 操作成功后显示成功提示
3. 操作失败显示错误信息
4. 删除等危险操作提供二次确认
5. 表单提交前进行数据验证，显示错误提示
6. 分页控件位于表格底部，支持页码跳转和每页条数调整

与其他模块的关联：
1. 用户状态影响登录模块，禁用状态的用户无法登录系统
2. 用户角色影响权限模块，不同角色的用户拥有不同的功能权限
3. 用户数据被订单、房源等其他模块引用，删除用户可能影响关联数据

四、个人信息管理功能

功能概述：
系统提供个人信息管理功能，用户可以查看和修改自己的个人资料，包括姓名、邮箱、手机号、性别、头像等信息，以及修改密码。

实现流程：
1. 页面加载时获取当前用户信息
2. 展示用户信息并提供编辑功能
3. 用户修改信息后提交保存
4. 提供密码修改功能，需要输入原密码和新密码

关键代码示例：
【vue3/src/views/backend/user/PersonInfo.vue：获取用户信息】
const fetchUserInfo = async () => {
  try {
    const userId = userStore.userInfo.id
    const res = await request.get(`/user/${userId}`, null, {
      showDefaultMsg: false
    })
    form.id = res.id || userStore.userInfo.id
    form.username = res.username || ''
    form.name = res.name || ''
    form.email = res.email || ''
    form.phone = res.phone || ''
    form.sex = res.sex || '男'
    form.avatar = res.avatar || ''
    form.idCard = res.idCard || ''
  } catch (error) {
    console.error('获取用户信息失败:', error)
    ElMessage.error('获取用户信息失败')
  }
}

【vue3/src/views/backend/user/PersonInfo.vue：保存用户信息】
const handleSave = async () => {
  if (!formRef.value) return
  try {
    await formRef.value.validate()
    saving.value = true
    await request.put(
      `/user/${form.id}`,
      {
        name: form.name,
        email: form.email,
        phone: form.phone,
        sex: form.sex,
        idCard: form.idCard
      },
      {
        showDefaultMsg: false,
        successMsg: '个人信息更新成功',
        onSuccess: (data) => {
          isEditing.value = false
          userStore.updateUserInfo({
            ...userStore.userInfo,
            name: form.name,
            email: form.email,
            phone: form.phone,
            sex: form.sex,
            idCard: form.idCard
          })
        }
      }
    )
  } catch (error) {
    console.error('更新用户信息失败:', error)
    ElMessage.error('更新用户信息失败')
  } finally {
    saving.value = false
  }
}

功能实现细节：
1. 个人信息页面分为两个部分：基本信息和密码修改
2. 基本信息区域支持上传和更换头像
3. 头像上传会先上传到服务器，获取URL后再更新用户信息
4. 用户名不可修改，其他信息可以修改
5. 密码修改需要输入原密码和新密码，并进行二次确认
6. 密码修改成功后会提示重新登录

五、密码修改和重置功能

功能概述：
系统提供密码修改和重置功能，用户可以修改自己的密码，管理员可以重置其他用户的密码。

实现流程：
1. 用户密码修改：输入原密码、新密码和确认密码，验证后更新
2. 管理员重置密码：在用户列表中选择用户，点击重置密码按钮，输入新密码

关键代码示例：
【vue3/src/views/backend/user/PersonInfo.vue：修改密码】
const handleChangePassword = async () => {
  if (!passwordFormRef.value) return
  try {
    await passwordFormRef.value.validate()
    changingPassword.value = true
    await request.put(
      `/user/password/${form.id}`,
      {
        oldPassword: passwordForm.oldPassword,
        newPassword: passwordForm.newPassword
      },
      {
        showDefaultMsg: false,
        successMsg: '密码修改成功',
        onSuccess: (data) => {
          passwordFormRef.value.resetFields()
          ElMessageBox.confirm('密码已修改，需要重新登录', '提示', {
            confirmButtonText: '重新登录',
            cancelButtonText: '取消',
            type: 'warning',
          }).then(() => {
            userStore.clearUserInfo()
            window.location.href = '/login'
          })
        }
      }
    )
  } catch (error) {
    console.error('修改密码失败:', error)
    ElMessage.error('修改密码失败')
  } finally {
    changingPassword.value = false
  }
}

【springboot/src/main/java/org/example/springboot/service/UserService.java：修改密码】
public void updatePassword(Long id, UserPasswordUpdateDTO update) {
  User user = userMapper.selectById(id);
  if (user == null) {
    throw new ServiceException("用户不存在");
  }
  if (!bCryptPasswordEncoder.matches(update.getOldPassword(), user.getPassword())) {
    throw new ServiceException("原密码错误");
  }
  user.setPassword(bCryptPasswordEncoder.encode(update.getNewPassword()));
  if (userMapper.updateById(user) <= 0) {
    throw new ServiceException("密码修改失败");
  }
}

功能实现细节：
1. 密码修改需要验证原密码
2. 新密码需要符合密码规则（长度在6-20个字符之间）
3. 密码存储采用BCrypt加密算法
4. 管理员重置用户密码时不需要原密码
5. 管理员重置密码通过用户邮箱和新密码进行
6. 密码修改成功后，用户需要重新登录

六、用户状态管理功能

功能概述：
系统提供用户状态管理功能，管理员可以启用或禁用用户账号。

实现流程：
1. 在用户列表中显示用户状态
2. 提供启用/禁用按钮
3. 点击按钮后更新用户状态

关键代码示例：
【vue3/src/views/backend/user/UserList.vue：切换用户状态】
const handleToggleStatus = async (row) => {
  const newStatus = row.status === 1 ? 0 : 1
  try {
    await request.put(`/user/status/${row.id}`, null, {
      params: { status: newStatus }
    })
    ElMessage.success(newStatus === 1 ? '已启用' : '已禁用')
    fetchUsers()
  } catch (error) {
    console.error('更新状态失败:', error)
  }
}

【springboot/src/main/java/org/example/springboot/controller/UserController.java：修改用户状态】
@PutMapping("/status/{userId}")
public Result<?> updateStatus(@PathVariable Long userId, @RequestParam Integer status) {
  User user = userMapper.selectById(userId);
  user.setStatus(status);
  userService.updateUser(userId, user);
  return Result.success();
}

功能实现细节：
1. 用户状态用不同颜色的标签显示（正常-绿色，禁用-灰色）
2. 启用/禁用操作会立即反映在用户列表上
3. 禁用状态的用户无法登录系统
4. 状态更改不影响用户的其他信息

七、头像上传功能

功能概述：
系统提供头像上传功能，用户可以上传和更改自己的头像。

实现流程：
1. 提供头像上传组件
2. 用户选择图片后上传到服务器
3. 服务器保存图片并返回URL
4. 更新用户头像信息

关键代码示例：
【vue3/src/views/backend/user/PersonInfo.vue：上传头像】
const customUploadAvatar = async (options) => {
  try {
    const { file } = options
    const formData = new FormData()
    formData.append('file', file)
    const uploadOptions = {
      headers: {
        token: localStorage.getItem('token') || '',
      },
      transformRequest: [(data) => data],
      successMsg: '头像上传成功',
      errorMsg: '头像上传失败',
      onSuccess: async (data) => {
        form.avatar = data
        await updateUserAvatar(data)
        options.onSuccess({ data })
      },
      onError: (error) => {
        console.error('头像上传错误:', error)
        options.onError(new Error(error.message || '上传失败'))
      },
    }
    await request.post('/file/upload/img', formData, uploadOptions)
  } catch (error) {
    options.onError(error)
    console.error('头像上传过程发生错误:', error)
  }
}

功能实现细节：
1. 头像上传限制文件类型为JPG和PNG
2. 限制文件大小不超过2MB
3. 头像更改后立即显示新头像
4. 头像信息存储在用户表的avatar字段中
5. 头像路径通过API基础路径拼接后使用

八、忘记密码功能

功能概述：
系统提供忘记密码功能，用户可以通过邮箱重置密码。

实现流程：
1. 用户输入邮箱和新密码
2. 系统验证邮箱是否存在
3. 验证通过后重置用户密码

关键代码示例：
【springboot/src/main/java/org/example/springboot/controller/UserController.java：忘记密码】
@GetMapping("/forget")
public Result<?> forgetPassword(@RequestParam String email, @RequestParam String newPassword) {
  userService.forgetPassword(email, newPassword);
  return Result.success("密码重置成功");
}

【springboot/src/main/java/org/example/springboot/service/UserService.java：忘记密码】
public void forgetPassword(String email, String newPassword) {
  User user = userMapper.selectOne(
    new LambdaQueryWrapper<User>()
      .eq(User::getEmail, email)
  );
  if (user == null) {
    throw new ServiceException("邮箱不存在");
  }
  user.setPassword(bCryptPasswordEncoder.encode(newPassword));
  if (userMapper.updateById(user) <= 0) {
    throw new ServiceException("密码重置失败");
  }
}

功能实现细节：
1. 忘记密码功能需要用户提供已注册的邮箱
2. 新密码需符合密码规则
3. 管理员可以帮助用户重置密码
4. 密码重置后用户需要使用新密码登录

九、与其他模块的交互

1. 用户模块与权限模块的交互：
   - 用户登录后根据角色加载不同的菜单和权限
   - 不同角色的用户具有不同的操作权限
   - 管理员可以管理所有用户，普通用户只能管理自己的信息

2. 用户模块与文件上传模块的交互：
   - 用户上传头像时使用文件上传服务
   - 文件上传服务保存图片并返回URL
   - 用户服务更新头像URL到用户信息中

3. 用户模块与JWT认证模块的交互：
   - 用户登录成功后生成JWT令牌
   - 请求接口时携带JWT令牌进行身份验证
   - JWT令牌过期后需要重新登录获取新令牌

十、用户角色管理

功能概述：
系统支持多种用户角色，包括管理员(ADMIN)、房东(LANDLORD)和租户(TENANT)。不同角色的用户拥有不同的权限和功能访问范围。

实现流程：
1. 用户创建时指定角色编码
2. 登录后根据角色加载对应的菜单和功能
3. 权限检查确保用户只能访问其角色允许的功能

关键代码示例：
【springboot/src/main/java/org/example/springboot/controller/UserController.java：根据角色获取用户】
@GetMapping("/role/{roleCode}")
public Result<?> getUserByRole(@PathVariable String roleCode) {
  List<User> users = userService.getUserByRole(roleCode);
  return Result.success(users);
}

【springboot/src/main/java/org/example/springboot/service/UserService.java：根据角色获取用户】
public List<User> getUserByRole(String roleCode) {
  List<User> users = userMapper.selectList(
    new LambdaQueryWrapper<User>()
      .eq(User::getRoleCode, roleCode)
  );
  if (users.isEmpty()) {
    throw new ServiceException("未找到该角色的用户");
  }
  return users;
}

功能实现细节：
1. 角色类型及权限范围：
   - 管理员(ADMIN)：系统最高权限，可管理所有功能和数据
   - 房东(LANDLORD)：可发布和管理自己的房源信息，查看预订记录
   - 租户(TENANT)：可浏览房源，提交预订，管理个人信息
2. 角色分配机制：
   - 用户创建时指定角色编码
   - 管理员可以修改用户角色
   - 用户不能自行修改自己的角色
3. 角色权限控制：
   - 前端根据用户角色显示对应的菜单项
   - 后端接口进行权限验证，防止越权访问
   - 不同角色用户的数据隔离，只能访问授权范围内的数据
4. 角色展示：
   - 用户列表中显示用户角色信息
   - 个人信息页显示当前用户角色

十一、文件上传模块与用户头像上传

功能概述：
系统提供文件上传功能，支持用户头像上传、房源图片上传等多种类型的文件上传。文件上传模块通过统一的接口实现不同类型文件的服务器存储和管理，确保文件安全存储并能被正确访问。

实现流程：
1. 用户在界面选择需要上传的图片文件
2. 前端验证文件类型和大小
3. 通过FormData将文件发送到后端指定接口
4. 后端接收文件，验证文件类型和大小，生成唯一文件名
5. 将文件保存到服务器指定目录
6. 返回文件的访问路径
7. 前端获取路径后更新用户头像信息

关键代码示例：
【springboot/src/main/java/org/example/springboot/controller/FileController.java：文件上传接口】
@Operation(summary = "文件上传")
@PostMapping("/upload/img")
public Result<?> upLoad(@RequestParam("file") MultipartFile file) {
  return fileService.upLoad(file, FileType.IMG);
}

【springboot/src/main/java/org/example/springboot/service/FileService.java：文件上传服务】
@Operation(summary = "文件上传")
public Result<?> upLoad(MultipartFile file, FileType fileType) {
  if (com.baomidou.mybatisplus.core.toolkit.StringUtils.isBlank(file.getOriginalFilename())) {
    LOGGER.error("文件不存在");
    return Result.error("-1", "文件不存在！");
  }
  LOGGER.info("upload FILE:" + file.getOriginalFilename());
  String path = FileUtil.saveFile(file, null, fileType.getTypeName());
  if (com.baomidou.mybatisplus.core.toolkit.StringUtils.isNotBlank(path)) {
    return Result.success(path);
  } else {
    return Result.error("-1", "文件上传失败");
  }
}

【springboot/src/main/java/org/example/springboot/util/FileUtil.java：文件保存工具】
public static String saveFile(MultipartFile file, String folderName, String baseDir) {
  String originalFilename = file.getOriginalFilename();
  assert originalFilename != null;
  long timestamp = System.currentTimeMillis();
  String extension = ""; // 文件扩展名，默认为空

  // 获取文件扩展名
  int dotIndex = originalFilename.lastIndexOf('.');
  if (dotIndex > 0) {
    extension = originalFilename.substring(dotIndex);
  }

  String dFileName = timestamp + extension;

  // 获取项目根目录路径
  Path projectRootPath = null;
  try {
    projectRootPath = Paths.get(FILE_BASE_PATH);
    Path fileDirectory = projectRootPath.resolve(baseDir);

    // 如果folderName不为null，则在指定目录后面加入folderName
    if (folderName != null && !folderName.isEmpty()) {
      fileDirectory = fileDirectory.resolve(folderName);
    }

    if (!Files.exists(fileDirectory)) {
      Files.createDirectories(fileDirectory); // 如果目录不存在，则创建目录
    }
    Path uploadFilePath = fileDirectory.resolve(dFileName);
    File uploadFile = uploadFilePath.toFile();

    file.transferTo(uploadFile);
    LOGGER.info("File saved at: {}", uploadFile.getAbsolutePath());
  } catch (IOException e) {
    e.printStackTrace();
    return null;
  }

  // 返回相对路径
  String relativePath = "/" + baseDir + "/" + (folderName != null && !folderName.isEmpty() ? folderName + "/" : "") + dFileName;
  return relativePath;
}

【vue3/src/views/backend/user/PersonInfo.vue：前端头像上传】
const customUploadAvatar = async (options) => {
  try {
    const { file } = options
    const formData = new FormData()
    formData.append('file', file)
    const uploadOptions = {
      headers: {
        token: localStorage.getItem('token') || '',
      },
      transformRequest: [(data) => data],
      successMsg: '头像上传成功',
      errorMsg: '头像上传失败',
      onSuccess: async (data) => {
        form.avatar = data
        await updateUserAvatar(data)
        options.onSuccess({ data })
      },
      onError: (error) => {
        console.error('头像上传错误:', error)
        options.onError(new Error(error.message || '上传失败'))
      },
    }
    await request.post('/file/upload/img', formData, uploadOptions)
  } catch (error) {
    options.onError(error)
    console.error('头像上传过程发生错误:', error)
  }
}

功能实现细节：

1. 文件上传类型支持：
   - 用户头像上传：支持JPG和PNG格式，限制2MB
   - 其他图片上传：支持常见图片格式，统一存储管理
   - 批量图片上传：支持多文件同时上传

2. 文件存储结构：
   - 基础路径：系统根据环境配置确定文件存储基础路径
   - 分类目录：根据文件类型分为img（图片）、videos（视频）等
   - 存储策略：使用时间戳命名，避免文件名冲突
   - 相对路径：返回相对路径，方便前端通过API地址访问

3. 文件访问权限：
   - 公共文件：如用户头像等可直接通过URL访问
   - 私有文件：需要通过权限验证才能访问
   - 临时文件：系统自动清理过期未关联的文件

4. 头像上传流程：
   - 用户在个人信息页面点击"更换头像"按钮
   - 弹出文件选择框，用户选择本地图片
   - 前端检查图片格式（JPG/PNG）和大小（≤2MB）
   - 通过FormData对象将文件发送到/file/upload/img接口
   - 后端接收文件，生成时间戳文件名并保存到/img目录
   - 返回文件相对路径，如/img/1747191858357.jpg
   - 前端获取路径后调用用户信息更新接口更新头像路径
   - 头像立即显示在界面上，完成更新

5. 文件上传安全性：
   - 文件类型验证：检查MIME类型和扩展名
   - 文件大小限制：防止超大文件上传占用服务器资源
   - 文件名处理：使用时间戳命名，避免恶意文件名
   - 路径安全：防止目录遍历攻击
   - 防盗链措施：配置防盗链策略保护上传文件

6. 错误处理：
   - 上传失败提示：友好展示上传失败原因
   - 日志记录：服务端记录文件上传日志
   - 空文件检查：防止上传空文件
   - IO异常处理：妥善处理文件读写异常

7. 多文件上传：
   - 批量上传接口：支持同时上传多个文件
   - 事务处理：确保多文件上传的原子性
   - 失败回滚：若部分文件上传失败，自动清理已上传文件

8. 文件删除功能：
   - 支持根据文件路径删除已上传文件
   - 头像更新时自动删除旧头像文件（可选）
   - 提供管理接口清理无效文件

9. 与用户模块的集成：
   - 头像上传后自动更新用户信息
   - 头像路径持久化到用户表
   - 用户界面实时显示最新头像
   - 头像缓存策略：合理设置缓存提高访问速度

10. 文件上传性能优化：
    - 图片压缩：可选择在前端或后端进行图片压缩
    - 文件存储采用异步方式
    - 大文件采用分片上传
    - 合理设置上传超时时间 